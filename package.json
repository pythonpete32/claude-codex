{
  "name": "claude-codex",
  "version": "0.0.1",
  "private": true,
  "description": "UI wrapper for Claude Code with DDD architecture",
  "workspaces": ["apps/*", "packages/*"],
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "test": "turbo test",
    "lint": "turbo lint",
    "format": "turbo format",
    "type-check": "turbo type-check"
  },
  "devDependencies": {
    "@biomejs/biome": "^1.9.4",
    "turbo": "^2.0.0",
    "typescript": "^5.7.0"
  },
  "packageManager": "bun@1.2.0",
  "engines": {
    "node": ">=18.0.0"
  }
}
